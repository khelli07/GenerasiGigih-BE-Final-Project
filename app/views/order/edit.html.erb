<div class="container" style="margin-top:13%; width:50%">
  <h2 class="mb-3">Editing Order ke-<%= @order.id %></h2>
  <%= form_with(model: @order, url: order_path(@order.id)) do |form| %>
   <div class="form-row mb-3">
    
    <div class="col">
    <label>Customer</label>
    <select name="cid" class="form-control">
      <option value="<%= @order.customer_id %>"><%= @order.customer_email %></option>
      <%= @customers.each do |c| %>
      <option value="<%= c.id %>"><%= c.email %></option>
      <% end %>
    </select>
    </div>

    <div class="col">
    <label>Status</label>
    <select name="status" class="form-control" aria-label=".form-select-lg example">
      <option value="0">NEW</option>
      <option value="1">PAID</option>
      <option value="2">CANCELED</option>
    </select>
    </div>
  </div>
    
  <input class="btn btn-success" type="submit" value="Selesai">
  <% end %>
</div>